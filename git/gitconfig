[url "git@github.com:"]
  insteadOf = https://github.com/
[alias]
  st = status
  ci = commit
  br = branch
  co = checkout
  d = diff
  df = diff
  dc = diff --cached
  lg = log -p
  who = shortlog -s --
